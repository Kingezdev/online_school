// Temporary file - StaffProfilePage.jsx needs to be imported and routed in App.jsx
// Add this import to App.jsx: import { StaffProfilePage } from './pages/StaffProfilePage.jsx';
// Add this routing to lecturer section: if (page==="staff profile") return <StaffProfilePage setPage={setPage} role={role}/>;
// Add this routing to admin section: if (page==="staff profile") return <StaffProfilePage setPage={setPage} role={role}/>;

export { StaffProfilePage } from './StaffProfilePage.jsx';
